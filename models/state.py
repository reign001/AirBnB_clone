#!/usr/bin/python3
"""defines the state class"""
from models.base_model import BaseModel

class state(BaseModel):
    """Represents a state.
        Attribute:
        name(str): the name of a string"""

        name = ""
